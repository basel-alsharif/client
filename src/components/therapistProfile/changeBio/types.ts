interface BioEditorProps {
    textBio: string;
    // eslint-disable-next-line no-unused-vars
    handleChangeTextBio: (value: string) => void;
    themeMode: string;
}

export default BioEditorProps;
